#include<iostream>
using namespace std;
int main(){
    int count,n;
    cout<<"Enter the limit: ";
    cin>>n;
    for(count=1;count<=n;count++){
    //    if(count%2==0){

 
    //     cout<<count<<endl;
    // }
    // cout<<count<<" Square is: "<<count*count<<endl;

    // if(count%2==0)>>>>>> even number
    if(count%2!=0)
    {
        // cout<<"Even no = "<<count<<endl;
        cout<<"ODD no = "<<count<<endl;

    }
    }
}